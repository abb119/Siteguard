# Initialize Git
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit: SiteGuard API with MLOps"

Write-Host "Repository initialized and files committed."
Write-Host "Next steps:"
Write-Host "1. Create a new repository on GitHub."
Write-Host "2. Run: git remote add origin <your-repo-url>"
Write-Host "3. Run: git push -u origin main"
